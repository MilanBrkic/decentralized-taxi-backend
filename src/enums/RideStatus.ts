export enum RideStatus {
  Created = 'created',
  Deployed = 'deployed',
  Started = 'started',
  Finished = 'finished',
  BeforeStartTimeout = 'before_start_timeout',
  BeforeEndTimeout = 'before_end_timeout',
}
